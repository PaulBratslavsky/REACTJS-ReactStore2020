import React from 'react';

export default function Product({featuredProducts}) {
    console.log(featuredProducts);
    return (
        <div>
            <h1>Show Products</h1>
        </div>
    )
}
